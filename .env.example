# Example environment configuration for MAVT
# Copy this file to .env and customize as needed

# Comma-separated list of app bundle IDs to track
# Examples:
#   - com.apple.mobilesafari (Safari)
#   - com.apple.Music (Apple Music)
#   - com.google.chrome.ios (Chrome)
#   - com.facebook.Facebook (Facebook)
MAVT_APPS=com.apple.mobilesafari,com.apple.Music

# Check interval (examples: 30m, 1h, 2h, 24h)
MAVT_CHECK_INTERVAL=1h

# Log level: debug, info, warn, error
MAVT_LOG_LEVEL=info

# Data directory for storing app information and updates
MAVT_DATA_DIR=./data

# Server settings (for future HTTP API)
MAVT_SERVER_HOST=0.0.0.0
MAVT_SERVER_PORT=8080
